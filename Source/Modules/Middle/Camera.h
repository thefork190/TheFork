#pragma once

#include <flecs.h>

namespace Engine
{
	// Describes camera related data for rendering
	struct Camera
	{
		
	};

	struct module
	{
		module(flecs::world& ecs); // Ctor that loads the module
	};
}
